
@{
    ViewBag.Title = "Order";
}

<div class="row">
    <div class="col-sm-12">
        <div class="card card-table">
            <div class="card-header">
                <h4 class="card-title">List of Users</h4>
            </div>
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table table-stripped table-center table-hover datatable">
                        <thead class="thead-light">
                            <tr>
                                <th>ID</th>
                                <th>Total Price</th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var prduct in Model)
                            {
                                <tr>
                                    <td>@prduct.Id</td>
                                    <td>@prduct.Price</td>
                                    <td class="text-end">
                                        <a href="/ecommerce/OrderDetails/@prduct.Id"
                                           class="btn btn-sm btn-white text-success me-2">
                                            <i class="far fa-edit me-1"></i> Details
                                        </a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>


